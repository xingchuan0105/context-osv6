# Grok 布局改造执行记录

## 已确认的基准

用户已验收 W0，并补充：资料面板可以默认收起，但笔记必须保留；顶栏添加资料直接打开文件选择器，选定文件后才展开资料状态。资料和笔记入口直接打开对应分区。同一工作区内收起面板或切换分区不清除笔记草稿。

基准：`grok-layout-w0/`，规范：`PRODUCT_IA.md` §5、`STYLE_BASELINE.md` 的 Rust 验收补充。样稿提交 `96b27479`，笔记/资料交互补充 `ab089cca`。

## W1：公共导航与个人聊天

- 新增复用的 NavigationState / NavigationRail / ContextTopBar；布局状态不接管会话数据。个人聊天已接入，后续页面逐批迁移。
- 导航 248/60px；桌面折叠偏好本机保存。折叠态仍有新对话/工作区快捷入口。手机导航抽屉支持遮罩、Esc、焦点循环与返回；主内容在抽屉打开时 inert，窗口尺寸变化关闭抽屉。
- 账户移到导航底部；上下文顶栏仅保留当前页面与通知。个人聊天移除全局分享菜单，设置/帮助仍在账户菜单；工作区入口保持 canonical。
- 输入区采用文本加单工具栏：44px 附件、联网、模型来源链接、发送/停止/重试。附件列表与错误独立占行，保留仅本轮说明；移除独立拖动手柄，保留自动增高、输入法和键盘发送行为。
- 新增专用 16px composer 圆角 token，导航行使用 8px；助手消息减少外围边框；空态引导与输入框聚合。
- 未新增后端接口；现有附件解析、64KiB 上限、重试附件复用、下一轮清除与工作区边界不变。

## 验证记录

本轮用户授权 W1 构建及定向验证 15–25 分钟。使用 jobs=2 和单 browser worker；无真实支付、真实模型调用或部署。

- 首轮 Rust 构建通过；lib 4、i18n 3、样式 guard 3 项通过。
- 首轮 70 个浏览器用例：69 通过，1 个导航可达性用例超时。原因是菜单已由 Esc 关闭后，旧 helper 仍等待并点击不存在的遮罩；改为检查可见性并显式验证关闭状态。
- 视觉复核发现空态 transcript 未收缩，导致输入区与标题间隔过大；已修正残留选择器，并新增空态间距断言。
- 第二轮 75 项中 71 通过，4 项浮层检查失败：账户菜单移至左栏后，旧遮罩仍扣掉 52px 顶栏高度，底部点击失效，并可能只关闭父抽屉而留下菜单打开状态。已将左栏账户遮罩设为完整视口，保留屏幕边缘点击复验，未降低断言。
- 第三轮 50 项中 48 通过，手机两项暴露账户菜单点击遮罩关闭后未返回焦点；补齐焦点返回，并保留明确的焦点断言。
- 最终 `cargo leptos build` 通过，14 项导航/浮层/附件定向复验全部通过。合计 75 个不同浏览器用例分批通过，包含 36 个聊天旅程和 6 个新增布局用例；没有宣称运行全站 72 入口矩阵。旧失败日志保留。
- 人工复核桌面空态、手机明暗主题、账户/导航浮层；本地 `http://localhost:3100/chat` 已运行最新构建。未进行真实模型调用、支付或部署。

日志：`/tmp/context-rust-w1-build.log`、`/tmp/context-rust-w1-unit.log`、`/tmp/context-rust-w1-browser.log`、`/tmp/context-rust-w1-final-build.log`、`/tmp/context-rust-w1-final-browser.log`。

收尾证据：`/tmp/context-rust-w1-close-browser.log`、`/tmp/context-rust-w1-focus-build.log`、`/tmp/context-rust-w1-focus-browser.log`；浮层截图位于 `/tmp/context-rust-visual/after/`。

## W2：工作区与侧栏反馈（2026-09-09）

用户「继续」授权执行此前说明的本批构建与验证。

- 工作区总览、工作台、统计接入 ApplicationLayout；会话在左侧，资料和笔记默认收起。宽屏资料面板 336px 并排，窄屏原生模态抽屉。旧工作台顶栏和固定右栏 CSS 已清除。
- 顶栏直接选择文件，选定后展开资料状态；取消选择不会额外打开资料区。原文件、链接、粘贴导入及资料预览、删除、检索范围保留。
- 折叠导航保留图标和「聊天 / 工作区」文字、当前位置标记；第二项固定工作区总览。移除由聊天历史推导的工作区主导航子列表；最近对话独立标记。工作区局部创建动作明确为「工作区新对话」。
- 笔记入口可直接打开；分区、面板切换保留草稿，浏览器页签缓存按用户和工作区隔离。链接离开和刷新/关闭会提示；后退/前进通过草稿恢复保护内容，不宣称完整拦截浏览器历史导航。保存成功清理草稿。
- 人工截图发现双编辑区与手机顶栏拥挤，已修复。富文本初始化成功后隐藏备用输入，组件释放时销毁编辑器及回调；手机使用资料/笔记/分享短标签，保留工作区标题。

验证证据：

- 最新 `cargo leptos build` 通过；Rust lib 4、i18n 3、样式 guard 3 共 10 项通过，jobs=2。
- 首批 25 项浏览器检查通过。扩展 69 项中 67 通过、2 项旧导航断言失败：旧测试要求工作区没有共享左栏，且两跳遍历只尝试 Dashboard。调整为检查唯一左栏和会话归属，并遍历实际可达的帮助/设置等中间页；保留原目标页面集合。
- 修正后 16 项工作区/导航回归全部通过；最后 10 项会话归属、创建工作区和明暗主题截图复验全部通过。累计 70 个不同用例分批通过，单 worker；并非全站 W5 矩阵。
- 人工复核 1280px 桌面浅色与 390px 手机深色笔记、手机关闭面板布局，以及实际 3100 折叠侧栏。四个宽度 390/768/1280/1440 × 明暗主题均留截图。
- 3100 已恢复最新 W2 前端，18091 为已有本地后端二进制；前端 HTTP 200，后端健康检查 200，同源工作区接口无凭据返回预期 401。没有重新编译后端，没有真实模型、支付或部署验证。用户验收待进行。
- code-review-graph 已更新；无后端/Subtex 文件纳入本批提交。

本批日志与截图保留在 `frontend_rust/target/acceptance/evidence/w2/`（本机忽略产物）。原日志 `/tmp/context-rust-w2-{build,build2,final-build,polish-build,unit,polish-unit,browser,final-browser,polish-browser,close-browser,graph}.log`；截图 `/tmp/context-rust-w2-visual/`。

## 后续批次

### W3：设置、分享与统计（2026-09-09 已交付本地验证版）

- 设置与用量接入 ApplicationLayout，提取 SettingsNavigation：桌面 200px 分类栏与最大 640px 表单，手机 `/settings` 展示分类、`?tab=` 进入详情，保留返回全部设置。BYOK、账单及用量深链保留；未知 tab 选中个人资料。未登录明确给出登录入口并隐藏退出登录动作。
- 分享中心、访问日志和互动分析共用 ShareNavigation 与应用壳；桌面分类和内容并排，手机三项紧凑分类。保留返回分享中心、工作区链路，不新增分享或支付接口。
- 公开知识库、作者页、邀请页接入无私人会话请求的 PublicLayout；保留公开资料问答、校验、公开资料开关、失效链接和邀请失败反馈。
- 已迁移页面移除 ProductChrome 引用，应用内无营销页脚；旧设置按钮导航及分享中心孤立横向导航已替换。原有配色、后端和 Next 不变。

验证：最新 Rust 构建通过；lib 4、i18n 3、style guard 3 共 10 项通过。首轮 46 浏览器用例 45 通过，1 项因仍假设工作区资料默认展开而失败；适配打开资料面板的操作后，保留 `workspace_id`、`capabilities`、`doc_scope` 原断言。复验 23 项、视觉/分类 10 项、最终登录/分类 16 项均通过，合计 48 个不同用例分批通过（单 worker）。模拟支付状态和匿名验证通过不代表真实支付或线上验证码验收。

人工复核设置、分享分析的 390/1280px 明暗主题截图；用量页、公开知识库桌面与公开作者手机截图。3100 设置页 HTTP 200，已刷新最新前端；真实账户业务、真实模型、支付、部署及全站 W5 尚未验收。代码关系图已更新。

证据：`frontend_rust/target/acceptance/evidence/w3/` 保存本机日志与截图（忽略产物）。日志源为 `/tmp/context-rust-w3-*.log`，截图源为 `/tmp/context-rust-w3-visual/`。

W4：管理、认证、商业、文档及其余公共页面。W5：全路由与异常状态验收。这两批尚未交付，不能将 W3 完成等同全站改造完成。
