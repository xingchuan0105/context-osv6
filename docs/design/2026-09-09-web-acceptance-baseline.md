# Rust Web 验收基线（2026-09-09）

本文件汇总已有验收与本次补验，作为 GPUI 开发的功能参照，不表示生产发布或所有平台验收完成。布局用户验收与自动测试、真实业务验证分别记录。

## 基线与提交

| 范围 | 提交 / 证据 | 状态 |
|---|---|---|
| W0–W5 Grok 布局 | `211ea244`、用户验收记录 `5360d9f9`；[开发记录](2026-09-08-grok-layout-development.md)、[入口矩阵](2026-09-09-grok-w5-matrix.md) | 用户已验收；72 入口 × 两宽度 × 两主题的 288 次模拟布局检查，代表截图人工复核；本批不重跑 |
| 真实 REST / 附件 / 工作区 / 分享 | `b9c9ba7b`；[真实旅程](2026-09-09-rust-real-journey-acceptance.md) | 已有真实业务验证 |
| 长流与嵌入计费 | `6ccba711`；[修复报告](2026-09-09-stream-timeout-and-embedding-billing.md) | 已有真实长文及嵌入账本证据 |
| 正文逐段显示与停止 | `076fa3d6`；[流式报告](2026-09-09-chat-progressive-answer.md) | 453 单测、6 条真实旅程分批通过；不是单次全量六条绿灯 |
| 审计 RLS | `96b15a49`；[修复报告](2026-09-09-provider-audit-rls.md) | 19 单测 + 1 真实数据库隔离回归；本次另补真实浏览器链路 |

## 本次真实 RAG 补验

运行既有 `real workspace note persists and uploaded source becomes searchable`，单 worker、真实 API / 上传处理 / 检索 / 模型，无 mock。1 条通过，旅程 57.8 秒、整个 Playwright 批次 58.8 秒。

覆盖：创建工作区 → 保存笔记并重载 → 上传合成资料并完成入库 → 勾选资料 → 问答命中 Amber Dock → 会话列表及历史重载正确。

- 工作区：`a9301316-7a75-4fed-8def-b93f1bcfc446`。
- 新增审计：`28bb5587-b37f-4103-8e56-86e57ab64998`，2026-09-09 16:36:03.931954 +08:00，`purpose=llm`、`provider=deepseek`、`action=resolve`。
- `workspace_id=null` 表示解析的是用户级密钥；以工作区所属用户和创建后的时间窗口核对，当前写入路径没有 request_id 级关联，不能宣称严格的一对一请求追踪。
- 本地 API 本次刷新以来日志未出现 `provider_secret_audit insert failed`。
- 首次审计核对脚本误把用途值写成 agent，导致脚本断言失败；实际业务测试已通过。按现有 llm 契约修正核对后通过，没有重发模型请求或修改产品业务代码。
- 证据：`frontend_rust/target/acceptance/evidence/rag-audit-live/`，含浏览器日志、截图和数据库核对 JSON。专用合成工作区保留供检查。

## 客户端实施约束

个人聊天不使用知识库；附件只进入本轮上下文，保留办公文件解析状态和容量错误。工作区资料持久化，笔记保留，资料面板默认收起且有快捷添加入口。首个 GPUI 闭环为登录、个人聊天、逐段输出、停止和历史恢复，之后再加入附件及工作区。

下一步应先检查现有客户端实现和接口复用情况，确定最小改造范围；本批没有开发 GPUI。支付仍使用现有统一入口。

## 验证边界

本地验收地址：http://localhost:3100/chat 。API 18091；本批未重新构建或部署。本次真实补验关闭已列出的审计 RLS 链路复验项；不承诺审计必达、所有模型延迟、任意复杂办公文件、真机软键盘、Safari 或真实支付通过。尚无针对本次后端修复的新用户验收结论。
