import { test } from "../../fixtures/run-context";

// Prompt library removed (product UX U7). Spec retained as tombstone so CI globs stay stable.
test.describe.skip("query library (product removed)", () => {
  test("placeholder", () => {
    // intentionally empty
  });
});
