/** Placeholder for static export (desktop BUILD_TARGET=desktop). */
export function generateStaticParams() {
  return [{ id: "_placeholder" }];
}

export default function LicenseDetailLayout({ children }: { children: React.ReactNode }) {
  return children;
}
