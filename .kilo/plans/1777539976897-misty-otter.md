# 剩余项目收敛与拆分计划

> **状态：历史计划（已废弃部分机制）**  
> 文中 `session_summary`、L2 会话摘要等描述**不再反映当前实现**。见 `avrag-rs/docs/adr/0007-react-phased-context-disclosure.md`。

## 背景

本计划用于接续当前 agent/search/RAG/auth/frontend 大型 dirty worktree 的合并前收敛。上一轮已完成 Chat/general 回归修复：

- `chat` 与 legacy `general` 都被视为 direct chat 后处理路径。
- `build_agent_request` 已恢复 `session_summary`、`user_preferences`、`working_memory` 注入。
- direct chat debug 不再硬编码 `memory_loaded=false` / `has_profile=false` / `has_working_memory=false`。
- 已通过 app、transport、frontend workspace、typecheck、`git diff --check` 和 added-line secret/log scan。

当前目标不是继续随意加功能，而是把剩余改动收敛到可 review、可提交、可回滚的几个独立单元。

## 当前状态

`git status --short` 显示 tracked 改动仍覆盖：

- Agent / Chat / streaming / GraphFlow：`avrag-rs/crates/app/src/chat/*`、`lib_impl/*`、`main_agent/mod.rs`、`app/src/agents/` 等。
- Search / Brave：`avrag-rs/crates/search/*`、`scripts/sync-keys.sh`、app config/bootstrap。
- RAG：`common/src/rag_execute.rs`、`common/tests/rag_execute_contract.rs`、`rag-core/src/runtime/*`、GraphFlow RAG tests。
- Auth / transport：`transport-http/*`。
- Contracts / frontend：`contracts/*`、`frontend_next/*workspace*`。
- 非主线：`ingestion/src/parser/mineru.rs`、`storage-milvus/src/lib.rs`、`llm/src/client.rs`、`.env.example`。
- 未跟踪产物：`.kilo/`、`.serena/`、`avrag-rs/.hermes/`、`avrag-rs/crates/app/src/agents/`。

## 成功标准

- 每个合并单元都有清晰主题，避免把 agent 主线与 MinerU、Milvus、LLM 默认配置等无关改动混在一起。
- Chat/general、Search/Brave、RAG v2、Auth、Frontend 的主路径各自通过 focused tests。
- `.env.example` 不包含无意义行尾 churn，不包含真实 key。
- 未跟踪运行产物默认不提交，只有明确有价值的 plan/report 可单独纳入。
- Brave live smoke 仅在 `/home/chuan/.config/avrag/keys.env` 配好 `SEARCH_API_KEY` 后执行，不在聊天中索要 key。

## 阶段 1：只读复核主线完成度

1. Chat/general 复核
   - 确认生产 response / trace / execution mode canonical 输出为 `chat`。
   - 确认 legacy `general` 只保留为输入 alias、测试兼容或 memory helper 文案。
   - 确认 stream path 没有重复 `Done` event。
   - 验证命令：
     - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p app general_mode_core_routes_through_unified_chat_agent -- --nocapture`
     - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p app chat_stream_routes_chat_through_unified_agent_service -- --nocapture`
     - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p app agents -- --nocapture`

2. Search/Brave 复核
   - 确认默认 provider 是 `brave_llm_context`。
   - 确认 Perplexity 只作为 legacy provider，不是生产默认依赖。
   - 确认 non-stream 与 stream 都经 `UnifiedAgentService -> WebSearchAgent`。
   - 确认 Brave evidence fetch 若为 buffered，debug payload 明确标记 `evidence_fetch_mode = brave_llm_context_buffered`。
   - 确认 streaming 只对 LLM synthesis token 发增量，不伪装 evidence fetch 为 token stream。
   - 验证命令：
     - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p avrag-search`
     - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p app web_search_agent -- --nocapture`

3. RAG v2 复核
   - 确认 raw planner output 在 normalize 前 validate，不默默修复非法 schema。
   - 确认 GraphFlow RAG 和 streaming RAG 都调用 `ensure_original_query_text_dense_item(...)`。
   - 确认 graph retrieval 无 structured hints / placeholder triplets 时不从 free-text query 自动扩图。
   - 确认 placeholder triplet 每条最多两个 placeholder，三个 placeholder validate error。
   - 确认 `RagAgent` fail-closed，不返回 fake success。
   - 验证命令：
     - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p common --test rag_execute_contract -- --nocapture`
     - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p avrag-rag-core`
     - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p app rag -- --nocapture`

4. Auth / frontend 复核
   - 确认 anonymous share chat asking questions 返回 `login_required` / unauthorized contract。
   - 确认 frontend mode canonical 为 `chat`，同时兼容旧 `general` 输入/trace。
   - 确认 `reasoning_summary_delta` decode/render 不破坏 token、done、citations 顺序。
   - 验证命令：
     - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p transport-http`
     - `cd /home/chuan/context-osv6/frontend_next && pnpm test tests/workspace/stream.test.ts`
     - `cd /home/chuan/context-osv6/frontend_next && pnpm test tests/workspace/ui-store.test.ts tests/workspace/workspace-chat-pane.test.tsx tests/workspace/workspace-right-rail.test.tsx`
     - `cd /home/chuan/context-osv6/frontend_next && pnpm typecheck`

## 阶段 2：拆分合并单元

不要把当前 worktree 直接作为一个提交或 PR。建议按以下顺序拆分：

1. Agent Chat/general canonical + memory parity
   - 包含：`app/src/agents/*`、`service_modes.rs`、`service_postprocess.rs`、`chat_streaming.rs`、`state_methods.rs`、相关 app/transport/frontend/contracts 中与 chat mode 直接相关的测试。
   - 排除：Search provider、RAG schema、MinerU、Milvus、DeepSeek 默认、`.env.example` 非必要 churn。

2. WebSearch Brave LLM Context
   - 包含：`crates/search/*`、`WebSearchAgent`、search config/default、`scripts/sync-keys.sh` 的 `SEARCH_API_KEY` 检查。
   - 验证：`avrag-search`、`app web_search_agent`、必要 transport stream tests。

3. RAG v2 contract/runtime hardening
   - 包含：`common/src/rag_execute.rs`、`common/tests/rag_execute_contract.rs`、`rag-core/src/runtime/*`、GraphFlow RAG normalize 与 tests。
   - 验证：common contract、rag-core、app rag/execute_plan focused tests。

4. Auth share-chat contract fix
   - 包含：`transport-http/src/middleware.rs`、`transport-http/src/handlers.rs`、相关 transport tests。
   - 验证：`cargo test -p transport-http`。

5. Frontend stream/chat alignment
   - 包含：`frontend_next/lib/workspace/*`、workspace components/tests、必要 contracts tests。
   - 验证：workspace vitest、typecheck、contracts tests。

6. 非主线单独 review
   - `avrag-rs/crates/ingestion/src/parser/mineru.rs`：单独 MinerU parser 变更。
   - `avrag-rs/crates/storage-milvus/src/lib.rs`：单独 Milvus schema compatibility 变更。
   - `avrag-rs/crates/llm/src/client.rs`：单独 DeepSeek reasoning/default 变更。
   - `avrag-rs/.env.example`：先消除行尾 churn，仅保留确需配置变更；如涉及默认 provider/model 切换，单独说明。

7. 运行产物和计划/report
   - 默认不提交 `.serena/`、宽泛 `.kilo/`、`avrag-rs/.hermes/runs/`。
   - 只提交明确需要保留的 plan/report，并在提交说明中写清用途。

## 阶段 3：最终验证矩阵

按拆分单元完成后运行：

1. Backend focused gate
   - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p app`
   - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p transport-http`
   - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p avrag-search`
   - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p common --test rag_execute_contract -- --nocapture`
   - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p avrag-rag-core`
   - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p contracts`

2. Frontend gate
   - `cd /home/chuan/context-osv6/frontend_next && pnpm test tests/workspace/stream.test.ts`
   - `cd /home/chuan/context-osv6/frontend_next && pnpm test tests/workspace/ui-store.test.ts tests/workspace/workspace-chat-pane.test.tsx tests/workspace/workspace-right-rail.test.tsx`
   - `cd /home/chuan/context-osv6/frontend_next && pnpm typecheck`

3. Static/final gate
   - `cd /home/chuan/context-osv6 && git diff --check`
   - `cd /home/chuan/context-osv6 && git status --short`
   - Added-line secret/log scan only，避免 pre-existing repo noise：
     - `cd /home/chuan/context-osv6 && git diff -U0 -- avrag-rs frontend_next contracts | rg -n '^\\+.*(console\\.(log|info|debug)|println!|dbg!|PERPLEXITY_API_KEY=.+|SEARCH_LLM_API_KEY=.+|API_KEY=.+|Bearer [A-Za-z0-9._-]+|sk-[A-Za-z0-9])'`

4. Optional live/E2E gate
   - 仅当 `/home/chuan/.config/avrag/keys.env` 有 `SEARCH_API_KEY` 后运行：
     - `cd /home/chuan/context-osv6/avrag-rs && bash scripts/sync-keys.sh --check`
     - `cd /home/chuan/context-osv6/avrag-rs && cargo test -p avrag-search brave_llm_context_live_smoke_returns_grounding_sources -- --ignored --nocapture`
   - Minsky E2E 仍需要 `E2E_EMAIL`、`E2E_NOTEBOOK_ID`、`E2E_DOCUMENT_ID`。

## 阶段 4：提交前原则

- 不回滚用户或其他 agent 的无关改动；拆分时只选择目标单元相关文件。
- 不把 `.env.example` 行尾 churn、generated artifacts 或 run outputs 混入主线提交。
- 不在代码或文档中写真实 API key。
- 如果某个拆分单元测试失败，先修该单元，不把失败留给后续单元掩盖。
- 如果涉及共享/导出符号或跨模块行为，实施阶段按项目 GitNexus 规则做浅层 impact check；若 GitNexus 不可用，在交付摘要中说明 fallback。

## 推荐下一步

1. 先执行阶段 1 的只读复核，确认 Search/RAG/Auth/Frontend 没有新的红灯。
2. 从 `Agent Chat/general canonical + memory parity` 开始拆分第一个 review 单元。
3. 第一个单元验证通过后，再按 Search、RAG、Auth、Frontend、非主线的顺序拆分。
4. 最后跑完整验证矩阵，并记录 live/E2E blocked 项。