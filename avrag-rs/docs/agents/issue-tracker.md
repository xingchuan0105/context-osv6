# Issue Tracker — Local Markdown

Issues for this repo are tracked as local markdown files under `.scratch/<feature>/`.

## Convention

- Each feature or bug gets a directory under `.scratch/`
- Issues are markdown files inside that directory
- File naming: `<number>-<short-description>.md`

## Creating an issue

Write a new markdown file under `.scratch/<feature>/` following the issue template.
