# Domain Docs

## Layout

Single-context — one global `CONTEXT.md` at the repo root plus `docs/adr/` for architectural decision records.

## Consumer rules

- Read `CONTEXT.md` first when you need domain background
- Check `docs/adr/` for past architectural decisions
