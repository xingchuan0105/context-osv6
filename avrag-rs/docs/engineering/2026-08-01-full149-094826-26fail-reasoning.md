# 全量 149 最近一轮(094826)26 题非 PASS 推理诊断(2026-08-01)

> run:`v2_20260801-094826`(全量②,PASS 123/149)。26 题非 PASS 逐题归因 + 代表性题推理过程反推。
> 说明:本轮跑在 R1/R2 评测修正**之前**;JUDGE_ERROR 已可经 rejudge 补判。

## 1. 根因归类(26 题 → 6 类)

| 类 | 题号 | 数 | 答案质量 | 性质 |
|----|------|-----|----------|------|
| **A. JUDGE_ERROR** | 5,23,37,38,44,60,122,127,133,135 | 10 | **全部完整**(q38/q44 诚实未覆盖、q122 计算 6245、q133 550/370) | judge API 故障,重判即 PASS(环境) |
| **B. UNGROUNDED** | 46,52,59,79,120 | 5 | corr 0.9-1.0,faith 0-0.6 | **答案正确但 judge 无支撑**——圈选不全/统计值无载体(R2/R1 修复对象) |
| **C. SELECTION_MISS** | 48,61,62,86,101 | 5 | 混合 | 表序错(q86)、半覆盖(q61)、坏运行(q62/q101 答案=代码调试)、ADR 内容错(q48) |
| **D. RETRIEVAL_MISS** | 65,107 | 2 | 诚实未覆盖(q65)/无回传(q107) | 检索未召回(语料无该数据?或检索失败) |
| **E. PARTIAL** | 103,104,118 | 3 | corr 0.7-0.9,faith 1 | 半覆盖/细节缺,答案质量好 |
| **F. REFUSAL_WRONG** | 141 | 1 | corr 0,faith 1 | 合理拒答但 golden 期望有答案(检索失败) |

## 2. 代表性题推理过程(从答案反推)

### q086 SELECTION_MISS(corr=0)——表序过滤推理错误
模型推理链:**按"责任人列 = LPDT"过滤概念阶段行** → 得行 5"团队培训 LPDT-04"为第一 → 结论 LPDT-04。
**错误点**:正确应为 LPDT-03(接受概念阶段任务书)。模型漏看了 LPDT-03 行(可能该行责任人列不同/未进回传),基于**不完整行集做了"第一个"归纳**。SKILL 的"出现顺序"规则没被正确应用——它该确认"LPDT 名下所有行的最小行号"而非"看到的第一个"。

### q046 UNGROUNDED(corr=1,faith=0)——推理正确但圈选不足(假失败)
模型推理链:grep 命中第 33 行 `* **Date**: 2026-06-06`(Status/Author 段落)→ 正确识别 2026-06-09 表格行属 ADR-0007 → **答案完全正确**。
**失败点**:只 `SELECTED: #1`(圈 1 个 chunk),judge 按引用核对认为 claim 无支撑 → UNGROUNDED。**R2 修复对象**(094826 跑在 R2 前)。

### q061 SELECTION_MISS(corr=0.5)——半覆盖 + 检索词
模型推理链:ADR-0004 找到原文"three contiguous slices"→ 3 Slice 正确;ADR-0009 仅见标题,`grep "Phase"` 0 命中 → 诚实未覆盖。
**失败点**:语料写 P1/P2/P3/P4 编号(无 "Phase" 单词),模型检索词不匹配。SKILL 中英双词引导已修。

### q062/q101 SELECTION_MISS(corr=0)——坏运行
模型答案 = **一段 Python 调试代码**(grep 各种"废弃/删除"模式循环打印),未收敛到最终答案。LLM 坏运行(与 q015/023/118 同类)。

### q065 RETRIEVAL_MISS(corr=0)——检索未召回
模型推理链:dense/lexical 均无 Salesforce CRM 份额相关命中 → 诚实说未覆盖 → 建议补充 IDC/Gartner 报告。
**失败点**:golden 期望 23%(语料可能有,模型检索词/召回未命中;q65 在 fail6 中 PASS 过,本轮漏召回属检索波动)。

### q141 REFUSAL_WRONG(corr=0,faith=1)——合理拒答但 golden 期望有答案
模型推理链:检索到急速冷冻技术原理段 → 但"国内理论研究现状""中美差距"关键词检索 0 命中 → 严谨拒答(区分"回传未覆盖"与"语料不存在")。
**失败点**:golden 期望有答案(语料应含此内容),模型检索词未召回或语料真缺 → 检索层问题,非模型编造。

### q120 UNGROUNDED(corr=0.9,faith=0.6)——双源正确但 fallback
模型推理链:知识库(#1/#2)RBF 抽成 2~5 倍 + 公开资料(OnDeck/StartupOwl)1.1~3 倍,双源都答了,corr=0.9。
**失败点**:cited=0(retrieved_fallback),judge 用检索兜底 context 判 faith=0.6 → 假失败边界(R1/R2 后应改善)。

## 3. 结论

- **~15/26 题是评测/环境问题**(JUDGE_ERROR 10 重判即 PASS + UNGROUNDED 5 假失败 R2 已修)→ 真实 PASS 应 ≈ 133-138/149
- **模型推理在多数题上是健康的**(诚实未覆盖、证据推理、双源对照);真正的模型短板集中在:**表序过滤(q086)、检索词匹配(q061/141)、坏运行(q62/101 采样)** ——均为 knowledge-base 层,手工 SKILL 打磨 + fail6 回归是对症循环
- 本轮结论与此前错题重跑一致,无新增模式
