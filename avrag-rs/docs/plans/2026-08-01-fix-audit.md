# 六轨修复审核报告（2026-08-01）

> 对象：`5f466116..1556a85f`（F1-F6 修复，review 报告 2052815b 之后）。
> 方式：主线独立复跑 gate + 3 个审核 subagent 语义复核（不信报告数字，实测对拍/探针/两轮复跑）。
> 总结论：**六轨全部修对，无修错、无弱化；必修项 1（.env.example 误标活变量）；建议项 7；登记项若干。**

## 0. 主线独立 gate（复跑实测）

| 项 | 结果 |
|---|---|
| `cargo test -p avrag-struct-supervision` | 63（50 lib + 13 bin）全绿（F1/F2 前为 42） |
| `cargo test -p avrag-rag-core --lib struct_query` | 18/18 全绿（F3 前为 14） |
| `cargo test -p ingestion --lib` | 85/85 全绿 |
| `cargo check -p avrag-worker` | 0 error |
| 三语料 parity（dump_grids vs --emit-grids） | ipd 1 / 白药 9 / 万科 306，行级 cells+notes 全等 |

## 1. 分轨 verdict

| 轨 | commit | verdict | 关键证据 |
|---|---|---|---|
| F1 失败语义线+安全 | `5f466116` | **修对** | H2：`case_insensitive` 双路真实应用，禁词表与 struct_query.rs 24 词逐项全等，`ReAd_CsV`/`READ_CSV_AUTO` 等混合大小写探针全拦；H1：`StructTablesOutcome` 四态推演无「旧库+新 _line_map」路径（Rebuilt 才重建，grids 空双保险）；M2：IO 路径无遗留 panic，Err → warn + OldKept，证据悬空 warn 文案如实；主线两处补丁（directives.rs cells / checks.rs 测试语法）经探针验证是正确修复非糊编译 |
| F2 校验口径 | `252aa63e` | **修对** | M3 万科 t114 **三侧对拍一致 2.29T**（Python pipeline / Rust 同 grids / Rust 自抽取；末位 ulp 级差异为 HashSet 求和序，远小于 0.1% 容差）；版式单测断言值手算复核正确；M1 auto_rotate keep 双侧同构，「auto_rotate+丢列」探针 8 项 checks 全过无 ragged；顺带项（sequence i128 / clip 省略号 / reparse_region [1:-1] / merge_tables 守卫）探针逐项等价；只新增测试，无既有断言改动 |
| F3 查询侧收紧 | `5f7d510a` | **修对** | 30 条查询矩阵：`read_csv_auto`/`read_parquet`/`glob` 全拦，逗号多关系全拒（`forbidden` + comma-join 消息）；`thread_id`/`spread` 不误伤；`read_count` 列名与字面量误伤属既有「只误伤不放行」先例且注释点名；catalog sample/headers 列数对齐（含系统列 fixture）；`ambiguous_relations` 仅真歧义出现 |
| F4 退役清理 | `ee4863fb` | **主体修对，1 必修** | 删除面无悬挂引用（grep 全仓）；`cargo check -p app --test product_e2e` 独立编译过；config.rs 4 死 key 生产零读取方实证；llm_real 断言非换皮（对照 build_parse_backend_summary 正等值）；**必修：`.env.example` 退役段混入 4 个活变量**（`MM_EMBEDDING_IMAGE_TOKEN_ESTIMATE`、`INGESTION_TRIPLET_ENABLED`、`INGESTION_VLM_TRIPLET_ENABLED`、`INGESTION_VLM_SUMMARY_ENABLED`——配置契约把活配置标成死配置，会误导删配） |
| F5 法务链 | `225b638f` | **修对** | AGPL 四环验证：目录物理删除；`cargo license` 实测 677 crates 树内无 pymupdf/liteparse/office-parser；notices 无误删（抽查 9 个无关条目均在，两文件 sync 一致，禁用词 grep=0）；`check-licenses.sh` 复跑 exit 0；page.tsx 的 markitdown(MIT) 多源事实核查通过 |
| F6 storage-pg 测试 | `1556a85f` | **修对** | 反弱化过检：GUC 包裹后断言对象/预期值原样，document_ir 双租户精确计数语义完整，claim 循环有上界且耗尽即 panic（不吞真失败）；**两轮独立复跑 22/22 全绿零自续污染**（旧病每轮净增 ≥2 queued 已愈）；队列 4 条污染 queued 已清、生产行无误删 |

## 2. 必修项（1）

1. **`.env.example` 退役段混入 4 个活变量**（A2 §8）：`MM_EMBEDDING_IMAGE_TOKEN_ESTIMATE`（llm/src/embedding.rs:56）、`INGESTION_TRIPLET_ENABLED`、`INGESTION_VLM_TRIPLET_ENABLED`（worker triplet_extraction.rs:440,444）、`INGESTION_VLM_SUMMARY_ENABLED`（indexing/env.rs:13）——移出「不再生效」段。
   **✅ 已修复（2026-08-01）**：修复时再发现 2 个同类活变量（`INGESTION_TRIPLET_MIN_CONFIDENCE`、`INGESTION_PAGE_RASTER_WITH_OCR`），共 6 个移出退役段；`E2E_PDF_MAX_PAGES` 标注为测试专用；退役段现仅剩 3 个真死变量（PDF_VISUAL_*、INGESTION_PDF_MAX_PAGES）。

## 3. 建议项（7）

1. `write_duckdb` 非原子（先删后写）——写半路失败时盘上可能是残缺新库且 `OldKept` 注释承诺「旧库保留」不成立；建议 temp+rename 或修正注释（A1 J1）。
2. `checks.rs` `detail_idxs` 用 HashSet → detail 数值末位跨进程抖动（容差内无害，快照引用会 flaky）→ `BTreeSet` 换确定性（A1 J2）。
3. `generate-e2e-test-registry.py` + `docs/e2e-test-registry.yaml` 同步删除 F4 移除的条目（含 2 条预存悬挂一并清）（A2 §4）。
4. 真跑一次 `generate-third-party-notices.sh` 刷新两份 notices（当前手工编辑版与真实树漂移 70 缺/33 幻影，预存）+ sync（A3 #1）。
5. `ir.rs` 变体级滞留注释与枚举级 doc 矛盾；`VisualRasterPdf` 补入 `is_historical_ir_only`（A2 §6）。
6. 手工清理 DB 卡死 processing 测试行 `c10ac548`（lease 到期后可被 claim；可顺带清 38 条 test-smell dead_letter）（A3 #2）。
7. `docs/runbooks/worker-dev.md:60,71` 死服务/死脚本引用更新（A3 #3；同 R2 B3）。

## 4. 登记项（不阻塞，合并自三路）

- validate_sql：`FROM "my table", t1`（含空格引号表名）逃出逗号拒——执行层单 doc 隔离兜底，形式漏洞；逗号拒注释「子句边界/括号外」字面略夸大。
- catalog：`headers.is_empty()` 极端情形 sample 回退 SELECT * 列数仍不齐。
- `config.rs:354` 死 `.env("PADDLE_OCR_RESULT_CACHE_ENABLED","0")`；`.env.example` 现役区 PADDLE_OCR_RESULT_CACHE_* 无读者（预存）。
- `session.rs` run_check 缺单语句守卫（struct_query.rs 已有）；`merge_tables` 对 0 行 grid `rows[1..]` 理论 panic（现有不变式不触发）。
- `docker-compose.milvus.yml:50` 过时注释；frontend `.next/`/`out/` 构建产物含旧 notices（build 自愈）；`frontend_next/e2e/...spec.ts` LiteParse 命名滞后（断言仍成立）。
- `checks.rs` sequence 注释称 Python isdigit 仅 ASCII——注释失实（Rust 行为安全）；merge_tables 比 Python 母本严格（一律拒 tid 入列，与母本行为分叉，prompt 示例未明示）。
- 3.5 阶段跳过时无日志（只能从 2.5 outcome 推断）；`struct_stage.rs:121` 注释表述别扭。

## 5. 结论

六轨修复质量高：所有原 BUG/DRIFT 逐项语义复核通过（含万科 t114 三侧 2.29T 实测对拍、禁词矩阵、反弱化逐测试过检），无修错、无测试弱化、无新回归。执行层面注意：F1/F3 subagent 曾中途暂停由主线接管补齐——审核已专门复核两个补丁点，均为正确修复。唯一必修是 `.env.example` 误标 4 个活变量（5 分钟修复）。建议项按 §3 排序随手清。
