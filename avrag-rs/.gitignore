# Git worktrees
.worktrees/

# Secrets
.env
!.env.example

# Build artifacts
/target/
/pkg/
*.wasm
# Cargo.lock is tracked so CI (rustc 1.93.1) gets a reproducible graph.
# Do not ignore it for this application workspace.

# Storage / runtime state
.run/.env.runtime
.qdrant-initialized
/storage/

# Legacy frontend (Leptos 0.7 skeleton) — superseded by frontend_rust/
/crates/web-ui/
/crates/web-sdk/
volumes/

# Python virtual environments
.venv/

# HeavyTail validation corpora (may be copyrighted; local-only)
/crates/heavytail/tests/corpus/*/*.txt
