[package]
name = "avrag-duckdb-store"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
version.workspace = true
description = "Owned structured-store connections with statically linked JSON and FTS"

[dependencies]
anyhow.workspace = true
duckdb = { version = "=1.10505.0", features = ["bundled", "json"] }
libduckdb-sys = { version = "=1.10505.0", features = ["bundled", "json"] }

[build-dependencies]
cc = "1"

[dev-dependencies]
tempfile = "3"
