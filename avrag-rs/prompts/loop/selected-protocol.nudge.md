[selected_protocol]
本 run 工具结果中已出现可引用的检索 alias。
用户可见终答中，每个有回传支撑的**文档侧主张**句末常见形态为 `（#n）`（半角 `(#n)` 亦可；n 为回传中的 alias）。
全文**末行**常见 `SELECTED: #n, #m`，与句内圈定一致。网页命中句侧常见 `[[web:n]]`。
宿主会把 `（#n）` 落到可点击引用，并去掉末行 SELECTED 协议行；句内无 alias 时用户侧只见文末角标，看不到「句级」出处。
[/selected_protocol]
