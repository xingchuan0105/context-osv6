Applies one inbox decision. `accept` moves the file into the suggested attached root (or an override `target_root` that is already attached); `reject` and `ignore` leave the file in the drop point and close the item; `undo` restores the last move for that item. No destination directory is created. After the same rule pattern is accepted several times, the result includes a convention-proposal payload for `subtex.correction_draft`.
