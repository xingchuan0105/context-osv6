Lists the inbox review queue and the drop-point configuration. Each pending item carries a suggested attached root when one mapped, a confidence tier (high / medium / low), and a reason. Files remain in the drop point until a later `subtex.inbox_decide` call. Low-confidence items stay in the queue without a target. Optional arguments add or remove a drop point (the directory must already exist) and set the autofill flag.
