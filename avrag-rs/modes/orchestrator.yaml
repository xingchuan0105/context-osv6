# Orchestrator agent: allocate only — no channel tools.
# STATUS: partially loaded — brain.rs reads `budget` and `temperature` from
# this file (see crates/app-chat/src/orchestrator/brain.rs). The remaining
# sections (skill_catalog / loop_exit / synthesis_output) are INERT today and
# kept for the future full controller config.
mode: orchestrator
system_prompt_base: prompts/orchestrators/orchestrator-base.md
tool_pool: []
skill_catalog:
  retrieve: []
  synthesis: []
budget:
  max_iterations: 6
  by_user_tier:
    free: 4
    pro: 6
    enterprise: 8
temperature: 0.4
loop_exit:
  require_evidence: false
  allow_content_early_stop: true
  skip_synthesis_on_direct_answer: true
synthesis_output:
  contract: prose_only
