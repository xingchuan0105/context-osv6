Follow AGENTS.md: make surgical changes only, no speculative abstractions, match existing style, remove unused imports, and verify. Use pnpm in frontend_next. Use cargo commands only in avrag-rs or relevant Rust subproject. For code changes in indexed repo, run GitNexus impact before editing symbols and detect_changes before commit/final review. Prefer explicit user-selected modes (RAG/Chat/WebSearch), strict evidence boundaries, and no raw chain-of-thought exposure.